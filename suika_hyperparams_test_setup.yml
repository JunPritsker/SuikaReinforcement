SuikaEnv-v0-test-setup:
  # algo: ppo
  n_envs: 16
  n_timesteps: !!float 5000 # Number of total environment steps to train for
  policy: 'MlpPolicy'
  env_kwargs:
    # render_mode: "human"
    debug: true
    # spatial_features: false